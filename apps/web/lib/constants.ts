import { Agent, CharacterClass, LogEntry } from './types';

export const INITIAL_PARTY: Agent[] = [];

export const MOCK_LOGS: LogEntry[] = [
    {
        id: 1,
        message: "System: Chat initialized.",
        type: 'info',
        timestamp: '00:00'
    }
];
